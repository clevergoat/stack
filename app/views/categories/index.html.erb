<h2>It's my Uber Homepage</h2>

<p>
	The time now is... <%= Time.now %>
</p>

<p>
	Username is <strong><%= @username %></strong> and they are <%= @age %> years old. In 20 years time they will be <%= @age + 20 %> years old.
</p>

<ul class="category-list">
	<!-- loop over the @categories -->
	<!-- note: no = tag as we don't want to print out the list -->
	
	<!-- for each thing in @categories -->
	<!-- call it "category" -->
	<% @categories.each do |category| %>
	
	<!-- print it out into a list -->
	<li>
		<div class="details">
			<%= link_to category.name, category_path(category) %>
		</div>
		<div class="actions">
			<%= link_to "Edit", edit_category_path(category), class: "edit" %>

			<%= link_to "Delete", category_path(category), method: :delete, class: "delete", confirm: "Are you sure you want to delete this category?" %>
		</div>

	</li>

	<% end %>
</ul>

<!-- link_to is more flexible for changing URLs later on -->

<%= link_to "New Category", new_category_path, class: "button" %>